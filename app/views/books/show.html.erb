<h2><%= @book.title %></h2>
<h3>Author:<%= @book.author %></h3>
<h5>Category: <%= @book.category_name%></h5>
<p>Description:<%= @book.description%></p>


    <%= render @book.reviews%>

    



    <div>
        <%= link_to "Add Review", new_book_review_path(@book)%>

    </div>

    



<%= link_to "Back", root_path%>






    <%= link_to "Edit", edit_book_path(@book) if @book.user_id == current_user.id%>
    <%= link_to "Delete", book_path(@book), method: :delete, data: {confirm: "Are you sure?"} if @book.user_id == current_user.id%>
    
  
