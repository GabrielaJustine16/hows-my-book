<!-- will dictate the structure of how this is out put into the book page-->

<p><%= review.rating%></p>
<p><%= review.comment%></p>

<!-- below is the book were dealing with and that instance-->


<%= link_to "Edit", edit_book_review_path(review.book, review) %>
<%= link_to "Delete", book_review_path(review.book, review), method: :delete, data: { confirm: "Are you sure?" }, class: "btn btn-custom" %>
